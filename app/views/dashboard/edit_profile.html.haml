- content_for(:title){"Expert Dashboard"}

- if @user.is_a? Expert
  = render "experts/guideline_page_modal"
= render partial: "dashboard/dash", locals: { main_menu: "profile/profile_form" }

:coffee
  pjax_dashboard()
