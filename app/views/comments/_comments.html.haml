- comments = commentable.comments.visible

.comments.margin-bottom
  %h4
    = commentable.decorate.comment_counting
  = render partial: "comments/comment", collection: comments, as: :comment
  = render partial: "comments/form", locals: {parent: commentable}

