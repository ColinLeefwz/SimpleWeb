<% commentable = @comment.commentable %>

$('<%=j render partial: "comment", locals: {comment: @comment}%>').insertBefore($("form"));
$(".comments").find("h4").html('<%= pluralize(commentable.comments.count, "Comment") %>');
$("form")[0].reset();
