%div{class: favorite_class(method, object)}
  - class_name = favorite_class(method, object, "-icon")
  = link_to image_tag("favorite.png", class: "#{class_name} tooltips", title: decide_tip_title(method), data:{placement: "bottom"}), link_path, remote: true
