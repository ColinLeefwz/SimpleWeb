= render "page_header", content: @session
.text-page-article-box.shadow
  .text-page-main-content
    -unless @session.cover.url.eql?"missing.png"
      .text-page-image
        = image_tag(@session.cover.url)
    %p= @session.description.html_safe
  .clear-fix
.range


