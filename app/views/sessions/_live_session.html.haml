= simple_form_for(@session, url: @url, remote: true) do |f|
  %h3 Create new session
  = f.input :title
  .categories
    = f.input :categories, collection: Category.all.map(&:name), as: :check_boxes 
  = f.input :format, collection: %w(Lounge Forum Livestreaming), as: :select
  = f.input :cover
  = f.input :video
  = f.label :start_time
  #datetimepicker.input-append.date
    = f.text_field :start_time, data: {format: "dd/MM/yyyy hh:mm:ss"}, type: "text" 
    %span.add-on
      %i{ data: { time_icon: "icon-time", date_icon: "icon-calendar"}}
  = f.input :time_zone
  = f.input :location
  = f.input :price
  = f.label :strategic_question
  = f.text_area :strategic_question
  %br
  %br
  %br
  = f.label :description
  = f.text_area :description

  = f.submit Session::COMMIT_TYPE[:draft]
  = f.submit Session::COMMIT_TYPE[:preview]
  = f.submit Session::COMMIT_TYPE[:submit]
