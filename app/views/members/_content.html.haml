.recommendation.white-background.shadow
  -if @recommendation
    Recommended Content
  -else
    Favorite Content
- @favorite_contents.each do |content|
  .jumbotron.container.shadow
    .col-sm-3
      .favorite-session-cover
        = link_to image_tag(content.cover.url), polymorphic_path(content)
        
    .col-sm-9
      = render "session_info", content: content
