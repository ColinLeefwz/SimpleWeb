<%= form_for(@ku, {:url => "/admin_kx_users/create_kx?id=#{params[:id]}"} ) do |f| %>
  <div class="field">
    姓名:<br />
    <%= f.text_field :name %>
  </div>

  <br/>
  <div class="field">
    来源:<br />
    <%= f.select :type, ['脸脸', '企博', '马甲'] %>
  </div>
  <br/>
  <%= f.submit '提交' %>
<% end %>